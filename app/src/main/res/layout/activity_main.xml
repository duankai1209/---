<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp"
        android:background="#AA000000">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="✨ 生活随机器 ✨"
                android:textSize="28sp"
                android:textColor="#FFFFFF"
                android:textStyle="bold"
                android:layout_marginBottom="24dp"
                android:shadowColor="#000000"
                android:shadowDx="2"
                android:shadowDy="2"
                android:shadowRadius="4" />

            <!-- 第一组 -->
            <Button android:id="@+id/btn_eat" style="@style/MainMenuButton" android:text="🍜 随机吃喝" />
            <Button android:id="@+id/btn_date" style="@style/MainMenuButton" android:text="💑 随机约会" />
            <Button android:id="@+id/btn_cloth" style="@style/MainMenuButton" android:text="👗 随机穿搭" />
            <Button android:id="@+id/btn_gift" style="@style/MainMenuButton" android:text="🎁 送礼指南" />
            <Button android:id="@+id/btn_lottery" style="@style/MainMenuButton" android:text="🎰 模拟抽奖" />

            <!-- 第二组 -->
            <Button android:id="@+id/btn_motto" style="@style/MainMenuButton" android:text="💪 励志格言" />
            <Button android:id="@+id/btn_scenic" style="@style/MainMenuButton" android:text="🏞️ 旅游推荐" />
            <Button android:id="@+id/btn_schedule" style="@style/MainMenuButton" android:text="📅 日程灵感" />
            <Button android:id="@+id/btn_sport" style="@style/MainMenuButton" android:text="⚽ 运动选择" />
            <Button android:id="@+id/btn_study" style="@style/MainMenuButton" android:text="📚 学习计划" />

            <!-- 第三组 -->
            <Button android:id="@+id/btn_weather" style="@style/MainMenuButton" android:text="🌤️ 模拟天气" />
            <Button android:id="@+id/btn_random_tool" style="@style/MainMenuButton" android:text="🎲 骰子/硬币" />
            <Button android:id="@+id/btn_user" style="@style/MainMenuButton" android:text="👤 个人中心" />
            <Button android:id="@+id/btn_setting" style="@style/MainMenuButton" android:text="⚙️ 设置" />
        </LinearLayout>
    </ScrollView>
</FrameLayout>